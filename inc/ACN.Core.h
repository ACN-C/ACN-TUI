#ifndef ACN_CORE_H
#define ACN_CORE_H

// Basic includes
#include <istream>
#include <string>

using namespace std;

namespace ACN {
  namespace CORE {
  class Init {
  public:
    Init();
    ~Init();
  private:
    
  };


  }
}
#endif // !ACN_CORE_H
